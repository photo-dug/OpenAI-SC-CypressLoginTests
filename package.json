{
  "name": "openai-sc-cypress-login-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress login security and robustness test suite",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:login": "cypress run --spec cypress/e2e/login.cy.js"
  },
  "devDependencies": {
    "cypress": "^13.15.2"
  }
}
